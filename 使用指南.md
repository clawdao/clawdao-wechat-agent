# 🏭 公众号工厂 — 使用指南

> 「觉知岛」公众号内容生产全流程工具链

---

## 📁 项目结构

```
公众号工厂/
├── AGENTS.md                      # 排版规范（必读！）
├── config.json                    # 全局配置（API/微信/封面/输出）
│
├── 📝 内容生成
│   ├── article_generator.py       # AI 写文章（需要 openai）
│   └── main.py                    # 全流程主入口
│
├── 🎨 图片生成（PIL 本地，无需 API）
│   ├── cover_generator.py         # 封面图 900×383（可直接运行）
│   ├── image_generator_enhanced.py # 封面 + 插图（可直接运行）
│   └── title_banner_generator.py  # 标题横幅
│
├── 🌐 图片生成（火山引擎 Seedream，需 API）
│   ├── seedream_generator.py      # AI 文生图背景
│   └── aideep_cover.py           # AI深度学堂封面模板
│
├── 📤 发布
│   ├── wechat_publisher.py        # 微信草稿箱发布模块
│   ├── publish_article.py         # 单篇发布
│   ├── publish_batch.py           # 批量发布
│   ├── publish_final.py           # 最终发布（含品牌头图）
│   └── publish_smart.py           # 智能发布（含图片上传）
│
├── output/                        # 输出目录（196 个文件）
│   └── 007-20260720/              # 按批次归档
└── images/                        # 设计规范参考
```

---

## 🚀 快速使用

### 前置条件

```bash
# Python 3.9+
# 安装 PIL（图片生成）
pip install Pillow

# 可选：AI 写文（需 openai）
pip install openai

# 可选：Seedream（需火山引擎 SDK）
pip install volcenginesdkcore
```

---

### 场景 1：生成封面图

```bash
cd /Users/imfly/Documents/projects/Codex-Agents-运营/公众号工厂

# 直接编辑 cover_generator.py 底部，添加你的标题
python3 cover_generator.py
# ✅ 输出: output/cover_你的标题.png
```

或代码调用：
```python
from cover_generator import generate_cover
generate_cover("你的文章标题", "主题分类")
```

---

### 场景 2：生成文中插图

```bash
python3 -c "
from image_generator_enhanced import generate_inline_image
# 三种风格: 'abstract' | 'ai-network' | 'zen-circle'
generate_inline_image('你的图片标题', style='abstract', width=640, height=360)
generate_inline_image('项目即一等公民', style='ai-network', width=640, height=360)
generate_inline_image('软件的上限', style='zen-circle', width=640, height=360)
"
```

---

### 场景 3：发布文章到公众号

**关键：文章必须是 Markdown 格式，插图用 `![描述](路径)` 语法！**

```python
from wechat_publisher import WeChatPublisher

pub = WeChatPublisher()

# ⚠️ 内容用 Markdown，不是 HTML！
md_content = """
# 你的文章标题

开篇引入文字...

## 一、第一个小节

正文内容，**加粗关键词**。

> 金句引用

![软件进化](output/007-20260720/illustration_01.png)

## 二、第二个小节

更多内容...

---
> 结尾金句
"""

pub.save_as_draft(
    title="你的文章标题",
    content=md_content,                          # Markdown 格式
    cover_image_path="output/封面图.png"          # 封面图路径
)
```

---

### 场景 4：完整工作流（选题 → 写文 → 配图 → 发布）

```bash
# Step 1: 选题（从 Obsidian 知识库获取大纲）
# 查看: ~/Documents/13-运营知识库/公众号工厂/爆款文案大纲/

# Step 2: 根据大纲写 Markdown 文章
# 保存为 output/article.md

# Step 3: 生成封面
python3 -c "
from cover_generator import generate_cover
generate_cover('你的标题', '分类')
"

# Step 4: 生成插图
python3 -c "
from image_generator_enhanced import generate_inline_image
for i, t in enumerate(['插图1', '插图2', '插图3']):
    generate_inline_image(t, style=['abstract','ai-network','zen-circle'][i])
"

# Step 5: 发布到公众号草稿箱
python3 -c "
from wechat_publisher import WeChatPublisher
pub = WeChatPublisher()
pub.save_as_draft(
    title='你的标题',
    content=open('output/article.md').read(),
    cover_image_path='output/cover_你的标题.png'
)
"
```

---

## ⚠️ 常见问题

### ❌ 插图不显示
**原因**：传给 `save_as_draft` 的是 HTML 内容，不是 Markdown。
**解决**：用 Markdown 格式写文章，图片用 `![描述](路径)` 语法。

### ❌ 封面图正常但文中插图空白
同上，`_markdown_to_html` 只识别 `![alt](path)` 语法。

### ❌ ModuleNotFoundError: openai / volcenginesdkcore
这些是可选依赖。PIL 封面生成器不需要它们，可以直接用。

### ❌ Seedream 图片生成失败
检查 `config.json` 中的 `seedream.access_key` 和 `secret_key` 是否有效。

---

## 📐 排版规范速查

| 元素 | 写法（Markdown） |
|------|------|
| 大标题 | `# 标题文字` |
| 小节标题 | `## 一、小节名` |
| 金句引用 | `> 金句文字` |
| 分隔线 | `---` |
| 加粗 | `**关键词**` |
| 插图 | `![描述](output/路径.png)` |

完整规范见 `AGENTS.md`。
