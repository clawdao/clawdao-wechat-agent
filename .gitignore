# ── 密钥与环境变量（严禁入库） ──
config.json
config.local.json
.env
.env.local
.env.*.local
*.local
credentials.json
# 注：.env.example 是脱敏模板，允许入库（白名单例外）

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── 备份/临时文件 ──
*.bak
*.tmp

# ── 产物 ──
output/*.png
output/*.md
outputs
images/
